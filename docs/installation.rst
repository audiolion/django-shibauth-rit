============
Installation
============

At the command line::

    $ easy_install django-shibauth-rit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-shibauth-rit
    $ pip install django-shibauth-rit
